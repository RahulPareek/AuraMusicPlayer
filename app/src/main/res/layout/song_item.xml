<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:id="@+id/include_song_item">


    <ImageView
        android:id="@+id/album_art_songs"
        android:layout_width="@dimen/list_album_art_width"
        android:layout_height="@dimen/list_album_art_height"
        android:layout_marginRight="25dp"
        android:layout_marginEnd="25dp"
        android:layout_alignParentTop="true"
        android:layout_below="@+id/firstline_songs" />

    <TextView
        android:id="@+id/firstline_songs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/song_name_size"
        android:ellipsize="end"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/album_art_songs"
        android:layout_toEndOf="@+id/album_art_songs"
        android:layout_toLeftOf="@+id/overflow_songs"
        android:layout_toStartOf="@+id/overflow_songs" />

    <TextView
        android:id="@+id/secondline_songs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:layout_below="@+id/firstline_songs"
        android:layout_toRightOf="@+id/album_art_songs"
        android:layout_toEndOf="@+id/album_art_songs"
        android:textSize="@dimen/artist_name_size"
        android:layout_toLeftOf="@+id/overflow_songs"
        android:layout_toStartOf="@+id/overflow_songs" />

    <ImageView
        android:layout_width="@dimen/overflow_width"
        android:layout_height="@dimen/overflow_height"
        android:id="@+id/overflow_songs"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />


</RelativeLayout>

